<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.ProjetDessin" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/view</item>
        <item name="colorPrimaryDark">@color/buttonColor</item>
        <item name="colorAccent">@color/textColor</item>
        <item name="android:windowBackground">@color/fond</item>
        <!-- Customize your theme here. -->
    </style>
</resources>